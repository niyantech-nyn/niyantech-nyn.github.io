# Normalise all text files to LF in the repository,
# so the project is consistent across Windows, macOS, and Linux.
* text=auto eol=lf

# Binary files — never convert
*.png  binary
*.jpg  binary
*.jpeg binary
*.webp binary
*.gif  binary
*.ico  binary
*.woff binary
*.woff2 binary
